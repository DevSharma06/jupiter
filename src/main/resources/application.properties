spring.application.name=jupiter

# MariaDB Connection Settings
spring.datasource.url=jdbc:mariadb://localhost:3306/ltc
spring.datasource.username=root
spring.datasource.password=Admin$!58
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# Show SQL (optional, to see the SQL generated by Hibernate)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG

#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true

#spring.flyway.enabled=true
#spring.flyway.locations=classpath:db/migration
#spring.flyway.baseline-on-migrate=true

# A strong, random secret key (minimum 256-bit for HS256). You can use a secure random generator.
app.jwtSecret=3d0b4e9a8f174f99b492a1a3171cfacd7f0a7b13675e47e79f9f4968c741e86e

# JWT expiration in milliseconds: 24 hours (1 day)
app.jwtExpirationMs=86400000

# Cookie name if you decide to store JWT in cookies (optional for bearer tokens)
app.jwtCookieName=jupiter_jwt_token
